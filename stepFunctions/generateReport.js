'use strict';

const generateReport = (params) => {

    return new Promise(async (resolve, reject) => {
        
        // lògica aquì

        resolve(params);
    });
}

module.exports = {
    generateReport
}