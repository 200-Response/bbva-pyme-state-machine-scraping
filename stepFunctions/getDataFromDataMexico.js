'use strict';

const getDataFromDataMexico = (params) => {

    return new Promise(async (resolve, reject) => {
        
        // lògica aquì

        resolve(params);
    });
}

module.exports = {
    getDataFromDataMexico
}